<div class="card"><div class="card-header"><h3 class="card-title"><i class="ti ti-history me-2"></i>{{ __('History') }}</h3></div><div class="table-responsive"><table class="table table-hover card-table"><thead><tr><th>{{ __('Date') }}</th><th>{{ __('User') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>
{% for entry in entries %}<tr><td>{{ entry.date_creation }}</td><td>{{ entry.user_login ?? entry.users_id }}</td><td>{{ entry.action }}</td></tr>{% else %}<tr><td colspan="3" class="text-center text-secondary">{{ __('No historical') }}</td></tr>{% endfor %}
</tbody></table></div></div>
